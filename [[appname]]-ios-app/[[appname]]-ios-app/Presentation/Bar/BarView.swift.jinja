import SwiftUI

struct BarView: View {
    var body: some View {
        Text("Bar")
    }
}

struct BarView_Previews: PreviewProvider {
    static var previews: some View {
        BarView()
    }
}
